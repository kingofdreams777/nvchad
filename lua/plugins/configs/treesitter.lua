local options = {
  ensure_installed = { "lua", "rust", "vue", "javascript", "typescript" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
